event.target = element that was clicked
event.currentTarget = element w/ event handler

1) Add padding to spans to increase hitbox
2) Cap length of fields to three decimals
3) Add hover and hand-pointer styling to equation
4) Add keyboard navigation
5) Style better
